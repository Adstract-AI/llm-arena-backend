# URL Guidelines

- Use clear model-based naming in route paths.
- Use UUID identifiers in URLs where IDs are path params.
- For nested resources, parent IDs should be explicitly named (example: `platform_id`).
- Group URL declarations by domain with clear comments.
